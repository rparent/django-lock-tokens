============
Installation
============

At the command line::

    $ easy_install django-lock-tokens

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-lock-tokens
    $ pip install django-lock-tokens
